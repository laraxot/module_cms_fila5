<?php

declare(strict_types=1);

describe('Password Confirmation', function () {
    test('password confirmation placeholder', function (): void {
        \PHPUnit\Framework\Assert::markTestSkipped('Serve il pannello con le rotte di conferma password: il test va scritto sul flusso, non sul segnaposto.');
    });
    test('password confirmation placeholder #2', function (): void {
        \PHPUnit\Framework\Assert::markTestSkipped('Serve il pannello con le rotte di conferma password: il test va scritto sul flusso, non sul segnaposto.');
    });
});
