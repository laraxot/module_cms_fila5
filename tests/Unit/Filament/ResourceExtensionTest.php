<?php

declare(strict_types=1);

describe('CMS Resource Extension', function () {
    test('resource extension placeholder', function (): void {
        \PHPUnit\Framework\Assert::markTestSkipped('Segnaposto senza asserzioni: serve il setup completo del pannello Filament.');
    });
    test('resource extension placeholder #2', function (): void {
        \PHPUnit\Framework\Assert::markTestSkipped('Segnaposto senza asserzioni: serve il setup completo del pannello Filament.');
    });
});
