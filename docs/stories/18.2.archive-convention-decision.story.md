---
title: "Archive Convention Decision: docs/_archive/ vs docs/archive/"
type: story
module: Cms
status: todo
created: "2026-09-06"
updated: "2026-09-06"
epic: 18
epic_title: "Sincronizzazione Drift + Dedup Docs"
related:
  - ../../../../docs/wiki/decisions/
  - ../coverage.md
---

# Archive Convention Decision (Story 18-2)

## Context

Epic 18 ("Sincronizzazione Drift + Dedup Docs") requires consolidating and
deduplicating documentation spread across 6+ modules (Ui, Job, AI, Cms, Xot,
Lang, etc.). A critical blocker: **what is the canonical convention for archived
docs?**

Currently, two conventions exist in parallel:

1. **`docs/_archive/`** — underscore-prefixed, signals "meta/internal" by convention
   (used in other contexts: `_bmad/`, `_components.json`, etc.).
2. **`docs/archive/`** — plain name, possibly `.gitignore`d or not depending on
   intent.

**This story blocks story 18-3** ("Implement Dedup + Archive Strategy"), which
needs an agreed convention before proceeding.

## Acceptance Criteria

### AC-1: Canonical Convention Decision

Decide on **one single convention** for archived docs across the monorepo:

- **Option A**: Use `docs/_archive/` (underscore prefix) as the canonical path.
  - Pros: Signals "internal/meta" by convention; aligns with existing `_bmad/`,
    `_components.json` patterns; sorts first in directory listings.
  - Cons: Underscore prefix slightly less discoverable without explicit knowledge.
  
- **Option B**: Use `docs/archive/` (plain name) as the canonical path.
  - Pros: Immediately discoverable; no special syntax; clear intent.
  - Cons: No visual signal that it is "meta"; potential confusion with regular
    doc folders; requires explicit `.gitignore` rule to prevent commits.

- **Option C**: Hybrid (one for per-module dedup, another for cross-module/root
  archive).
  - Requires detailed decision on which use case maps to which path.

**Decision outcome**: `AC-1-DECISION` (to be filled in during execution).

### AC-2: Communicate Decision to Team

Once AC-1 is decided:

1. **Document the decision** in `docs/decisions/` (see AC-3 for path/format).
2. **Announce via `docs/chat/`** (pre-announcement before story 18-3 begins).
3. **Unblock story 18-3** by providing the agreed convention.

This prevents story 18-3 from starting with ambiguous assumptions.

### AC-3: Document in `docs/decisions/`

Create a decision record at:
`docs/decisions/2026-09-06-archive-convention.md`

**Required sections**:

- **Title**: "Archive Convention: Canonical Path for Deduped Docs"
- **Date**: 2026-09-06
- **Decision**: The chosen convention (AC-1 outcome).
- **Rationale**: Why this convention wins (3-5 sentences).
- **Implementation Scope**:
  - Which modules use this convention.
  - How `.gitignore` should handle archived docs (if applicable).
  - Example file paths in at least 2 modules (e.g., `Modules/Cms/docs/_archive/`,
    `Modules/Xot/docs/_archive/`).
- **Related Artifacts**:
  - Link to story 18-2 and epic 18.
  - Link to `docs/chat/` announcement.
- **Supersedes**: (if any prior conflicting decision exists).

## Scope (Cross-Modulo)

This story affects documentation strategy across:
- Modules/Cms
- Modules/Ui
- Modules/Job
- Modules/AI
- Modules/Xot
- Modules/Lang
- (and any other module with dedup-pending docs in memory)

The **decision is singular** (one convention), but the **application is cross-modulo**
(story 18-3 implements it everywhere).

## Dependencies

- **Prerequisite**: None (independent decision).
- **Blocks**: Story 18-3 ("Implement Dedup + Archive Strategy").
- **Related**: Memory `memory/bmad-story-numbers-need-a-registry.md` (docs
  organization patterns).

## Verification

- [ ] AC-1: Consensus decision recorded (in commit message or pull-request comment).
- [ ] AC-2: Announcement posted to `docs/chat/` (timestamped).
- [ ] AC-3: Decision record written to `docs/decisions/2026-09-06-archive-convention.md`
  and committed.
- [ ] Story 18-3 can proceed without ambiguity.

## Notes

- **Non-blocking conflict**: Both `docs/_archive/` and `docs/archive/` currently
  exist in Cms/docs (as discovered in 2026-09-06 memory). This story **does not
  resolve the existing state** — it sets the **forward convention** for new dedup
  work. Cleanup of existing mixed state is out of scope (assign to 18-3 or a
  follow-up story if needed).
- **Timing**: Execute AC-1 and AC-2 as a tight pair (decision → announcement),
  then close. AC-3 is documentation work (can be done immediately after AC-1).
